name: 'job_intelligence'
version: '1.0.0'
config-version: 2

profile: 'job_intelligence'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table